# ITAM Linux Asset Discovery Agent Dependencies

# HTTP client for communication between Agent and API Server
requests>=2.31.0
urllib3>=2.2.1

# Telemetry data collection (CPU, Memory, Disk usage metrics)
psutil>=5.9.8

# Cross-platform /etc/os-release parsing
distro>=1.9.0

# Security and encryption libraries
cryptography>=42.0.5

# Screen capture
mss>=10.2.0
pillow>=10.0.0

# X11 cursor capture (Linux)
python3-xlib>=0.31

# WebRTC screen sharing
aiortc>=1.7.0
av>=11.0.0
aiohttp>=3.9.0

# Offline storage via SQLite
# (sqlite3 is stdlib, no pip dep needed)

# Optional: for building standalone binaries
pyinstaller>=6.5.0
